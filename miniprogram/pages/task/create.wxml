<view class="page-wrap">
  <view class="card">
    <view class="section-title">创建任务</view>

    <picker range="{{b}}" range-key="name" bindchange="{{c}}" style="margin-top:12rpx">
      <view class="picker-field">{{a}}</view>
    </picker>

    <input class="field" placeholder="任务标题" value="{{d}}" bindinput="{{e}}" />
    <input class="field" placeholder="地点（可选，默认可定位）" value="{{f}}" bindinput="{{g}}" />
    <input class="field" placeholder="图片地址（可选）" value="{{h}}" bindinput="{{i}}" />

    <picker mode="date" bindchange="{{k}}" style="margin-top:10rpx">
      <view class="picker-field">截止日期：{{j}}</view>
    </picker>

    <button class="btn-primary" style="margin-top:12rpx" bindtap="{{l}}">创建任务</button>
    <button class="btn-stack" bindtap="{{m}}">查看回忆归档</button>
  </view>
</view>
